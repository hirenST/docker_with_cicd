name: DOCKER SETUP

on:
  workflow_dispatch:
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - name: replace custom addons
        run: |
          cp -r ./etc /home/st11/Project
